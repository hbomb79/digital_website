// The slider needs to have a timer that automatically changes the slide, although this slider must pause while hovered (reset)
// To do this the _G.timer object must be used to not only ensure clearing on page change, but also to have a global place to clear and remove timers.
// 
// The slider will be registered and stored, 

(function(){
	// Self Invoking Anonymous Function
})();